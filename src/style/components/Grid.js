import styled from 'styled-components';
// import PropTypes from 'prop-types';

const Grid = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    align-items: stretch;
`;

Grid.propTypes = {
    
};

Grid.defaultProps = {

};

export default Grid;